<apex:page cache="false"
  standardStylesheets="false"
  showHeader="false"
  sidebar="false"
  controller="pkb_Controller"
  language="{!IF($CurrentPage.parameters.l == null, 'en_US', $CurrentPage.parameters.l)}"
  applyBodyTag="false"
  applyHtmlTag="false"
>

<!-- this empty page simply invokes the PKB site template and gives us a page to set the Site homepage to -->
        <apex:composition template="ISSP_pkb_template">
            <apex:define name="body"></apex:define>
        </apex:composition>

</apex:page>