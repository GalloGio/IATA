public class clsIFAP_CaseInProcessControllerExtension {

	/*private final Case aCase;

	public Boolean isRendered{
		get {
			if (aCase.Status == 'Ready to process') return true;
			return false;
		}
	}

	public String MessageEn{
		get{ return 'Upload is confirmed subject to verification by IATA.  Accounts will be required to be re-uploaded if the file format is incorrect, information is missing and/or the information provided is incorrect.'; }
	}

	public String MessageFr{
		get{ return 'Le téléchargement sera confirmé après vérification par IATA. Un nouveau téléchargement  sera nécessaire si le format du fichier n’est pas correct, s’il manque des informations ou si celles-ci sont erronées.'; }
	}

	public String MessageEs{
		get{ return 'Recepción correcta de los documentos, sujeta a la verificación por parte de IATA.  Se requerirá un nuevo envío de estas cuentas si el formato de los ficheros es incorrecto, o si  la información es incompleta o incorrecta.'; }
	}

	public clsIFAP_CaseInProcessControllerExtension(ApexPages.StandardController stdController){
		this.aCase = (Case)stdController.getRecord();
	}*/
}
