<apex:component controller="CW_EmailTemplateData" access="global">
    <apex:attribute name="stationData" type="String" description="Station ID" assignTo="{!stationId}" access="global" />
    <apex:outputPanel rendered="{!accountToApproveUrl != null}">
        <p style="line-height: 1.8rem; color: #1e32fa; font-weight: 600;">
            ATTENTION: Station Approval requires ACCOUNT approval – To approve ACCOUNT <a style="color: #1e32fa;" href="{!accountToApproveUrl}">click here</a> and change Account Type to “Other”
        </p>
    </apex:outputPanel>
</apex:component>